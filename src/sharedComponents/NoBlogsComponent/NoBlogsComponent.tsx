import React from 'react';
import SectionHeader from '../SectionHeader/SectionHeader';

const NoBlogsComponent: React.FC = () => {
	return <SectionHeader header={"Seems like you haven't created any blog.Try adding new blog"} />;
};

export default NoBlogsComponent;
